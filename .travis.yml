language: java

jdk: openjdk8

script:
  - echo "Hola Travis"
  - mvn package
  - chmod 400 "acceso_instancia.pem"
  - ssh-keyscan -H 3.134.28.16 >> ~/.ssh/known_hosts
  - ssh-keyscan -H ec2-3-134-28-16.us-east-2.compute.amazonaws.com >> ~/.ssh/known_hosts
  - scp -i "acceso_instancia.pem" target/Netflix-1.0-SNAPSHOT.war ubuntu@ec2-3-134-28-16.us-east-2.compute.amazonaws.com:/tmp
